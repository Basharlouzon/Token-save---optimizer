# Plan: Update README.md for Latest Commit

## Context
The latest commit (`8904d2d`) added:
- `.claude/skills/brainstorming/SKILL.md` — A "brainstorming" skill for Claude Code that enforces a design-first workflow (no implementation without an approved design spec)
- `.claudecode` + `CLAUDE.md` — Tokenso optimizer rules for Claude Code (these are already documented in the README compatibility table)
- Updated stats and repo-map (not user-facing content)

The README currently at `2.2.0` already covers Claude Code in the tool list and compatibility table, but does **not** mention the brainstorming skill.

## Changes

### 1. Add brainstorming skill to Quick Start tool list
**File:** `README.md` line ~45 (tool selection wizard)

Add a note or line item showing the brainstorming skill ships alongside the Claude Code integration. The wizard display already shows Claude Code as option `1` — we should note that selecting Claude Code also installs the brainstorming skill.

**Edit:** After the Claude Code line in the wizard example, or as a footnote, add:
```
1) Claude Code          (.claudecode + CLAUDE.md + brainstorming skill)
```

### 2. Add "Claude Code Skills" subsection to Compatibility
**File:** `README.md` — in the `## 🤝 Compatibility` → `### AI Coding Agents` section

After the Claude Code table row, add a new subsection or footnote:

```markdown
### Claude Code Skills

| Skill | Location | Description |
|---|---|---|
| Brainstorming | `.claude/skills/brainstorming/SKILL.md` | Design-first workflow — enforces spec approval before any implementation |

> The **brainstorming skill** ships automatically when you select Claude Code during `tokenso install`. It prevents agents from jumping straight to code by requiring an approved design spec first.
```

### 3. Optionally: Add brainstorming mention to "How It Works" Inject layer
**File:** `README.md` line ~78

Expand the Inject description to mention skills:
```
2. **Inject** — Drops search diet rules and memory protocol instructions into your AI tool's config files (`.cursorrules`, `.clinerules`, `.claudecode`, etc.). For Claude Code, it also installs skills like **brainstorming** that enforce design-first workflows. The AI automatically follows them on every session.
```

## Summary of edits
| # | Section | Change |
|---|---|---|
| 1 | Quick Start wizard display | Add `+ brainstorming skill` to Claude Code line |
| 2 | Compatibility section | New "Claude Code Skills" subsection |
| 3 | How It Works → Inject | Mention brainstorming skill |
